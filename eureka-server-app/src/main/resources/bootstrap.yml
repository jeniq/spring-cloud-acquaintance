spring:
  application:
    name: eureka-server-service
  cloud:
    config:
      uri: http://localhost:8888
server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false